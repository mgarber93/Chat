const {User} = require('../../../db');

const getAll = (req, res) => {
  res.sendStatus(200);
};

const getOne = (req, res) => {
  res.sendStatus(200);
};

const update = (req, res) => {
  res.sendStatus(200);
};

module.exports = {
  getAll,
  getOne,
  update,
};